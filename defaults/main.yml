---

# awscli_users:
#   backup:  # user on the system, should exist on passwd file
#     default:  # aws profile file name
#       region: us-east-1
#       aws_access_key_id: changeme  # key definition
#       aws_secret_access_key: changeme

awscli_defaultconfig:
  region: us-east-1
  output: json
